// Placeholder for the upcoming Canvas renderer extraction.
// The current production renderer still lives inside ../app.js.

export class CanvasRenderer {
  init() {}
  resize() {}
  render() {}
  destroy() {}
}
