# 전쟁의 시대 (Age of War) — 프로젝트 문서

## 개요

아이소메트릭 **실시간** 전술 전쟁 게임.  
플레이어는 준비 단계에서 부대 목표를 지정하고, 전투 단계에서 실시간으로 시뮬레이션이 진행된다.  
순수 브라우저 기반(HTML + Vanilla JS), 외부 라이브러리 없음.

> **주의:** 이 게임은 턴제(turn-based)가 아니다. 고정 타임스텝 실시간 시뮬레이션이다.

---

## 파일 구조

```
Age of war/
├── GAME_DESIGN.md         # 이 문서
├── _history/              # 버전 아카이브 (zip)
├── tools/                 # 개발 도구 스크립트
└── web/
    ├── index.html         # 진입점 — app.js를 <script>로 로드
    ├── app.js             # 실제 서비스 파일 (모든 로직 단일 IIFE)
    ├── styles.css         # 전체 스타일
    ├── game-logic.js      # 모듈형 버전: 게임 로직 (ES module)
    ├── renderer.js        # 모듈형 버전: 렌더링 (ES module)
    ├── input.js           # 모듈형 버전: 입력 처리 (ES module)
    ├── main.js            # 모듈형 버전: 진입점 (ES module)
    ├── assets/terrain_tiles_v3/   # 지형 스프라이트 에셋
    └── data/speech_bubbles.json   # 말풍선 텍스트 데이터
```

### 두 가지 구현체

| 파일 | 역할 | 비고 |
|------|------|------|
| `app.js` | **프로덕션** — index.html이 로드하는 단일 파일 | IIFE로 전체 포함 |
| `main.js` + `game-logic.js` + `renderer.js` + `input.js` | **모듈형 개발 버전** | ES module, `<script type="module">` 필요 |

두 버전의 로직은 동일하게 유지해야 한다. 기능을 변경할 때는 **두 버전 모두** 수정한다.

### 서버/빌드 전환 방침

2026-05-08부터 프로젝트를 로컬 실행 전제에서 서버와 빌드가 있는 웹 프로젝트로 전환한다.

- 빌드 도구: Vite
- 서버: Node.js + Express
- 현재 실행 기준: `web/app.js` 유지
- 향후 공식 소스 위치: `web/src/`
- 렌더링 전환: 기존 Canvas 렌더러 동작을 보존한 뒤 PixiJS WebGL 렌더러를 점진 도입
- 데이터 저장: 정적 JSON 유지 후 서버 저장 데이터는 JSON → SQLite 순서로 확장
- 세부 기술 설계는 `TECH_DESIGN.md`에 기록한다.

---

## 게임 흐름

### 두 단계

```
planning (준비)  →  [전투 개시 버튼]  →  live (전투)
```

- **planning**: 부대 목표 위치·방향 지정 가능. 시뮬레이션 정지.
- **live**: 실시간 시뮬레이션 진행. 이동·전투·AI가 매 프레임 업데이트됨.

### 시뮬레이션 루프

- 고정 타임스텝: `SIMULATION_STEP = 1/30` 초
- 최대 누적 스텝: `MAX_SIMULATION_STEPS = 4` (프레임 드롭 시 캐치업)
- 속도 배수: 1× / 2× (UI 토글)
- AI 전략 갱신: live 단계에서 3초마다
- 승패 조건 충족 시 즉시 정지하지 않고 `live` 시뮬레이션을 3초 더 유지한 뒤 최종 종료 및 결과 화면 표시

---

## 좌표계

### 타일 좌표 (게임 로직)
- 모든 게임 로직은 **타일 단위** 부동소수점 좌표 사용
- 맵 크기: `MAP_WIDTH = 240`, `MAP_HEIGHT = 160` 타일
- 단위: 타일, 타일/초

### 아이소메트릭 변환 (렌더링)
```
isoPoint(x, y) → { x: (x - y) × (tileW / 2),
                    y: (x + y) × (tileH / 2) }
```
- `tileW` = 현재 줌의 타일 픽셀 너비 (16 / 20 / 24)
- `tileH = Math.floor(tileW / 2)`

### 줌 레벨
```js
ZOOM_LEVELS    = [16, 20, 24]   // 픽셀/타일
DEFAULT_TILE_W = 24
```

### 에셋 기준 해상도
- 지형 에셋은 **타일 실제 크기(최대 줌 기준 24×12 px)** 에 맞게 제작한다.
- 과거 128×64 제작 후 축소 방식은 **폐기**됨.

---

## 렌더링 파이프라인

### 청크 기반 지형 캐싱
- 맵을 `CHUNK_TILES = 16` 단위 청크로 분할
- 줌이 변경되거나 스프라이트 로드 상태가 바뀌면 청크 캐시 전체 무효화
- 각 청크는 오프스크린 Canvas에 미리 그린 뒤 메인 Canvas에 blit

### 지형 렌더링 — 다중 패스

에셋 경로: `./assets/terrain_tiles_v3/`

### 평지 오브젝트

- `assets/objects/object_sheet_tiles/object_00.png`~`object_15.png`를 사용한다.
- 경계가 아닌 `plain` 타일에 약 2% 확률로 오브젝트 1개를 고정 배치한다.
- 오브젝트 종류는 타일 좌표 해시 기반으로 16종 중 하나를 선택해 청크 캐시 재생성 중에도 위치와 종류가 유지된다.

| 패스 | 조건 | 내용 |
|------|------|------|
| Pass 1 | 항상 | 색상 다이아몬드 폴백 + 산 그림자 다이아몬드 |
| Pass 2A | 스프라이트 로드 완료 시 | 1×1 center 타일 이미지 (지형별 다중 variant) |
| Pass 2B | 스프라이트 로드 완료 시 | 3×3 연속 블록에 대형 베이스 텍스처 적용 |
| Pass 2C | 항상 | 경계 타일 3레이어 마스크 합성 (상위·하위 지형 블렌딩) |
| Pass 3A | 스프라이트 로드 완료 시 | **험준산악**: 16×16 연속 mountain 블록에 대형 이미지 (`mountain_forest_16.png`) |
| Pass 3 | 스프라이트 로드 완료 시 | 산 타일에 나무 오브젝트 랜덤 배치 (화가 알고리즘) |

**험준산악 생성 조건**
- 16×16 타일이 모두 `mountain`이고 단일 청크(16×16) 내에 완전히 포함될 것
- 위 조건 충족 시 **50% 확률**로 생성

### 스프라이트 렌더링
- 유닛: 10×14 px 픽셀아트, 코드 내 팔레트 배열로 정의 (외부 이미지 없음)
- 스케일: `game.tileW / 20` (최대 줌 24에서 1.2×)
- `imageSmoothingEnabled = false` (픽셀아트 nearest-neighbor)

### 미니맵
- `MAP_WIDTH × MAP_HEIGHT` 픽셀 오프스크린 Canvas (1px = 1타일)
- 초기화 시 1회 생성 후 고정

---

## 장수(General) 스탯

장수는 부대당 1명이며, 랜덤 생성된다.

| 스탯 | 범위 | 역할 |
|------|------|------|
| `power` | 45–100 | 공격력 결정 |
| `leadership` | 40–100 | 혼란도 감소, 유닛 이동속도 보너스 |
| `charm` | 35–100 | 스킬 쿨타임 단축, 혼란도 감소 |
| `troops` | 병과/인구 예산에 따라 변동 | 실제 병사 수 |
| `troopType` | infantry/cavalry | 장수 부대의 병과 |

---

## 병과 시스템

장수 한 명은 하나의 병과만 지휘한다. 한 장수의 부대 안에서 보병과 기병을 섞을 수 없다.

| 병과 | 인구 비용 | 기본 병력 | 근접 공격 | 이동속도 | 근접 방어 | 원거리 방어 | 스킬 | 밀도 제한 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|------|------|
| 보병 | 1 | 10,000 | ×1.0 | ×1.0 | ×1.0 | ×1.0 | 장수별 선택 가능 | 촘촘/보통/넓게 |
| 기병 | 4 | 2,500 | ×3.0 | ×2.0 | ×4.0 (+10 고정) | ×3.0 | 기합만 가능 | 보통/넓게 |

- 전체 병력 조정 예산은 실제 병사 수가 아니라 인구 예산 `50,000` 기준이다.
- 병력 조정 화면에서 기병 1명을 추가하면 인구 4를 사용한다.
- 빠른 전투의 아군 첫 번째 장수와 다섯 번째 장수는 기본 기병으로 시작하며, 각 병력은 2,500명이다.
- 기병은 6프레임 `ancient_cavity_helmet_walk.png`, `ancient_cavity_helmet_walk_blue.png` 스프라이트시트를 사용한다.
- 기병 표시 높이는 기본 줌에서 25px이며, 프레임 비율에 맞춰 가로 폭도 함께 확장한다. 진형 간격과 충돌 반경은 보병보다 크게 적용하되, 충돌 반경보다 진형 간격을 조금 더 좁게 잡는다. 기병은 바닥 그림자를 표시하지 않는다.
- 기병은 원거리 공격을 수행하지 않는다.

---

## 지형 속성

| 타입 | 이동 배수 | 방어 보정 | 비고 |
|------|-----------|-----------|------|
| plain | 1.0× | 0 | |
| road | 1.6× | 0 | |
| river | 0.3× | **-2** | |
| wetland | 0.55× | **-1** | 강·시작구역 교차 지점에 생성 |
| mountain | 0.70× | **+3** | |

방어 보정은 해당 타일 위 유닛의 `unitDefense()` 계산에 직접 가산된다.

---

## 속도 설정 (Speed)

| 설정 | 이동(타일/초) | 방어 보정 | 공격 배수 | 반응사거리(타일) |
|------|:---:|:---:|:---:|:---:|
| STOP | 0.0 | **+2.0** | 0.95× | 2.5 |
| SLOW | 0.30 | **+1.0** | 0.98× | 3.2 |
| NORMAL | 0.9 | 0 | 1.02× | 4.0 |
| FAST | 1.35 | **-1.0** | 1.05× | 5.0 |

- **방어 보정**: `unitDefense()`에 직접 가산
- **공격 배수**: 현재 실제 이동속도를 FAST 기준으로 0~1 정규화 → 0.95~1.05 사이 선형 보간
- **반응사거리**: 적을 탐지해 교전을 시작하는 반경. 혼란도 ≥ 0.6부터 점진 감소, 혼란도 1.0에서 최대 -20%.
- **느림(SLOW)**: 현재 진형 방향을 유지한 채 이동한다. 목표 위치나 적 추적 명령을 받아도 이동 중 진형 방향을 자동 회전하지 않는다. 선택 시 "현재 진형을 유지한채 이동하라." 말풍선을 표출한다.

---

## 진형 밀도 (Density)

| 설정 | 유닛 간격(타일) | 근접 방어 보정 | 원거리 피해 배율 |
|------|:---:|:---:|:---:|
| TIGHT | 0.60 | **+0.75** | **×1.15** (밀집 → 원거리 취약) |
| NORMAL | 0.82 | 0 | ×1.00 |
| WIDE | 1.08 | **-0.75** | **×0.85** (분산 → 원거리 강함) |

- 근접 방어 보정: `unitDefense()`에 직접 가산
- 원거리 피해 배율: `densityInfo[targetFormation.density].rangedDefenseMult` — 적의 밀도 기준으로 원거리 피해량에 곱함

---

## 전투 공식

### 방어력 `unitDefense(formation, unit)`

```
방어력 = max(0,
  2                           ← 기본값 (고정)
  + speedInfo[speed].defense  ← 속도 보정 (-1.0 ~ +2.0, FAST/STOP 기준)
  + densityInfo[density].defense ← 밀도 보정 (-0.75 ~ +0.75)
  + terrainInfo[tile].defense ← 지형 보정 (-2 ~ +3)
  - disorder × 2              ← 혼란 페널티
)
```

### 공격력 `unitAttack(formation)`

```
공격력 = max(0,
  (15 + power/100 × 15)       ← 기본값 (power 범위: 45~100 → 21.75~30)
  × (1 - disorder × 0.25)     ← 혼란 페널티 (disorder 1.0 → 75% 공격력)
  × speedMult                  ← 속도 배수 (0.85 ~ 1.20)
)
```

### 근접 전투 데미지 (거리 < 0.85 타일, 매 프레임)

```
데미지 = max(0,
  unitAttack(공격자) × attackerBonus × facingMult
  - unitDefense(방어자) × defenderBonus
) × dt
```

| 변수 | 값 |
|------|-----|
| `attackerBonus` | 선두행 + 슬롯 내: `_firstRowBonus` / 슬롯 내: 1.1 / 기타: 1.0 |
| `defenderBonus` | 선두행 + 슬롯 내: 1.5 / 슬롯 내: 1.1 / 기타: 1.0 |
| `facingMult` | 정면(±45°): **1.25** / 측면: **1.0** / 후방: **0.75** |

### 원거리 전투 데미지 (쿨타임 1초, 방어 무시)

```
데미지 = (15 + power/100 × 15) × 0.2 × facingMult
```

- 근접의 약 20% 수준
- `attackerBonus`, `defenderBonus` 미적용 (방어력 무시)
- 조건 1 — 반응사거리 내 적이 있을 때 (근접 불가 시)
- 조건 2 — 7타일 이내 적, 반응사거리 밖 (별도 독립 판정)

### 선두행 보너스 `_firstRowBonus`

```
_firstRowBonus = 1 + 생존유닛수 / (선두행유닛수 × 50)
```

선두행 판별: 진형 로컬 y좌표가 최전선에서 `spacing × 0.5` 이내인 유닛.

---

## 혼란도 (Disorder)

혼란도는 0~1 범위의 실수이며, 매 프레임 생존률로 재계산된다.

```
rawDisorder = max(0, (0.6 - 생존률) / 0.6)   ← 생존률 60% 미만부터 누적
disorder    = rawDisorder
              × max(0, 1 - leadership/100 × 0.35 - charm/100 × 0.20)
```

| 생존률 | 기본 disorder |
|:------:|:-------------:|
| 100% ~ 60% | 0 |
| 40% | 0.33 |
| 20% | 0.67 |
| 0% | 1.0 |

**페널티 요약**

| 항목 | 페널티 |
|------|--------|
| 방어력 | `-disorder × 2` |
| 공격력 | `× (1 - disorder × 0.25)` |
| 유닛 이동속도 | `× max(0.1, 1 - disorder × 1.4 × (1 - NORMAL/FAST))` — disorder 50%에서 NORMAL 속도와 동일 |
| 반응사거리 | disorder ≥ 0.6부터 점진 감소 `× (1 - (disorder - 0.6) × 0.5)` — disorder 1.0에서 최대 -20% |

**혼란도 증가 (`disorderAccum`)**

- 기본 누적속도: `0.001/초 × 지형배수 × (1 - charm/100 × 0.5)`
- 정위치 이탈 비율 기반 가속: `distMult = 1 + outRatio × 8.0`
  - 정위치 기준: 슬롯에서 0.8 타일 이내 (`POSITION_DEFENSE_THRESHOLD`)
  - 이탈 0% → 1×, 이탈 50% → 5×, 이탈 100% → 9×
- 기합(氣) 스킬 발동 시 즉시 -0.1 감소

---

## 병력/데미지 판정 기준

- 화면의 유닛 1개는 실제 병사 100명을 압축해서 표시한다.
- 병력이 100으로 나누어떨어지지 않을 경우 마지막 표시 유닛만 남은 병사 수를 수용한다.
- `unit.damage`는 해당 유닛 안에서 발생한 실제 병사 손실 수이며, 범위는 0~100이다.
- 근접/원거리/화공/수공 피해는 실제 적용된 damage만큼 장수의 처치/손실 통계에 누적한다.
- HUD 병력바, 우측 패널 표시 병력, 전투 결과 화면의 처치/손실/잔존 병력은 `sum(100 - unit.damage)` 기준으로 계산한다.
- 유닛은 `damage >= 100`일 때만 화면에서 전사 처리되고, 후열 보충 및 시체 흔적 처리가 발생한다.

## 이동 관련

- 앵커 이동속도: `speedInfo[speed].move × 지형배수 × 병력페널티` (혼란도 영향 없음)
- 유닛 이동속도: `FAST.move × 지형배수 × (1 + leadership/100 × 0.4) × 병력페널티 × 혼란도패널티`
- 병력페널티: `max(0.65, 1 - (병력수/50000) × 0.35)` — 병력 많을수록 느림, 병력 손실 시 빨라짐
- 혼란도패널티: `max(0.1, 1 - disorder × 1.4 × (1 - NORMAL/FAST))` — disorder 50%에서 NORMAL 속도
- 기병 충돌 반발력: 기병 진형과 충돌 시 반발력 2.5배 (`cavalryKnockMult = 2.5`)
- 충돌 Hard radius: `UNIT_RADIUS × 2 = 0.54 타일`
- 충돌 Soft radius: `1.8 타일`
- 공간 해시 셀: `SPATIAL_CELL_SIZE = 4 타일`

---

## AI 전술 판정 거리

- 근접 전투 전환: 12 타일 이내
- 산악 방어 포지션: 20 타일 이내
- 측면 기동: 30 타일 이내
- AI 갱신 주기: 3초

---

## 시야(Fog of War)

전투 단계(`live`)에서만 적용된다. 준비 단계에서는 전체 노출.

- 각 아군 진형은 생존 병력 비율에 비례한 시야를 갖는다
- **시야 한계**: 유효거리 60타일. 가변시야 없음.
- **산악 지형**: 광선이 산악 타일을 통과할 때 0.5타일 이동 비용 = 1.0 유효거리 (2배 소모). 산악이 많은 방향일수록 시야가 줄어들고 포그 폴리곤 모양이 변형됨.
- **구현**: 관측자→대상 경로를 0.5타일 단위로 샘플링해 유효거리 계산 (`effectiveDistance()`). 포그는 120방향 레이캐스팅 폴리곤으로 렌더링 (`castRay()`).
- **안개 투명도**: 기본 42% 불투명

---

## 스킬 시스템

장수 전용 액티브 스킬. 전투 단계에서만 사용 가능.  
모든 스킬 발동 시 공통으로 혼란도 0.1 즉시 감소.

### 스킬 배정 확률

병력 조정 단계에서 아군 장수의 스킬을 선택할 수 있다.

- 기본 선택 가능: 기합(氣), 신속(疾), 사수(守)
- 장수별 선택 가능: 화공(火), 수공(水), 신궁(弓)
- 장수별 선택 가능 스킬은 `assets/portraits/generals.json`의 `optionalSkills`에 기록한다.

---

### 기합 (氣)

- **쿨타임**: `200 - charm/100 × 60` 초 (charm 35~100 → 179~140초)
- **효과**: 진형 내 생존 유닛 전체에 `kihapTimer = rand(4, 6)초` 부여
- `kihapTimer > 0`인 유닛은 선두행 공격 보너스(`_firstRowBonus`) 적용
- **시각**: 황금 발광 이펙트 (ellipse + shadowBlur)

---

### 신속 (疾)

- **쿨타임**: `45 - charm/100 × 13` 초 (charm 35~100 → 40~32초)
- **효과**: 12초간 부대가 지형·속도 제한 없이 빠르게 기동 (`swiftTimer = 12.0`)
- 화공·수공 대비 짧은 쿨타임으로 기동 지원에 특화

---

### 사수 (守)

- **쿨타임**: `90 - charm/100 × 27` 초
- **효과**: `7 + leadership/100 × 3`초간 정위치 유닛의 방어 보너스 적용 후 최종 방어값을 2배로 증가 (`guardTimer = 7~10초`)
- **정위치 조건**: 유닛이 자기 슬롯 목표 위치에서 0.8타일 이내

---

### 신궁 (弓)

- **쿨타임**: `100 - charm/100 × 30` 초
- **효과**: 5초간 모든 유닛의 원거리 공격 사거리를 2배로 증가 (`archeryTimer = 5.0`)
- 반응사거리 기반 원거리 교전과 별도 원거리 사격 판정 모두 2배 범위 적용
- `optionalSkills`가 있는 장수만 선택 가능

---

### 화공 (火)

- **쿨타임**: `120 - charm/100 × 36` 초 (charm 35~100 → 107~84초)
- **효과**: 최전방 유닛 기준 전진 방향 4타일 앞에 4×4 화염 파티클 생성
  - 파티클 개별 지속시간: 10~20초 랜덤
  - 매 1초마다 랜덤 방향으로 0.5타일 이동
  - **강(river)·습지(wetland) 타일에서는 파티클 생성 및 이동 불가**
- **피해**: 매 1초, 파티클 반경 0.55타일 내 모든 유닛(아군 포함)에 **+20 피해**
- **부가 효과**: 발동 시 부대 속도 강제 STOP
- **유닛 반응**: 파티클 1.3타일 이내 접근 시 회피 방향 힌트, 1.2타일 이내 시 강한 반발력

---

### 수공 (水)

- **쿨타임**: `160 - charm/100 × 48` 초 (charm 35~100 → 143~112초)
- **효과**: 2초 후 강(river)·습지(wetland) 위의 모든 유닛에 **+20 피해** (아군 포함, 1회)
- **시각**: 발동~피해 적용 전까지 강·습지 타일에 흰색 반투명 다이아몬드(`rgba(255,255,255,0.3)`) 펄싱
- **말풍선**: 피해가 실제로 적용된 부대에만 표출

---

## UI 구조

### HUD (상단 카드)
- 장수 이름 + 병력 수(숫자) + 병력 바 차트
- 장수 능력치 텍스트 없음
- 상단 장수 선택 카드는 전투 화면 위 레이어로 유지하며 클릭 가능성을 보장한다.

### 우측 패널 (수직 구조)
1. **장수 정보**: 이름 + 무력/통솔/매력 뱃지 (선택된 부대 기준)
2. **부대 지표**: 병력 수 + 혼란도 % + 병력 바 차트
3. **이동 속도**: [정지] (강조) → [느림 / 보통 / 빠름]
4. **정렬 / 밀도**: 기존 동일
5. **스킬**: 배정된 스킬 버튼 1개 (쿨타임 fill 시각화). 스킬 종류에 따라 아이콘·라벨 변경

### 병력 조정 화면
- 아군 장수 카드에서 병력 수와 스킬을 함께 조정한다.
- 모든 장수 카드는 동일한 스킬 버튼 배열(기합/신속/사수/화공/수공/신궁)을 표시한다. 선택할 수 없는 특수 스킬은 비활성화한다.
- 적군은 병력과 스킬을 읽기 전용으로 표시한다.

### 전투 화면 로딩 마스크
- 전투 화면 진입 시 캔버스 영역에 로딩 마스크를 표시한다.
- 지형 스프라이트와 유닛/이펙트 이미지 로딩 시도가 완료되면 마스크를 서서히 숨긴다.
- 오른쪽 컨트롤 패널 버튼은 포커스를 받지 않도록 하여 Space/Tab 단축키 입력을 방해하지 않게 한다.

### 속도 복귀 로직
- STOP 진입 시 직전 속도를 `formation.prevSpeed`에 저장
- STOP 상태에서 [정지] 버튼 재클릭 또는 Space 키 → prevSpeed로 복귀 (기본: NORMAL)
- STOP 상태에서 [느림/보통/빠름] 버튼 직접 클릭 → 해당 속도 즉시 적용, prevSpeed도 해당 속도로 갱신
- STOP 상태에서 배속 토글 → prevSpeed로 복귀

---

## 향후 작업 예정

- [x] 지형 에셋 적용 완료 (`./assets/terrain_tiles_v3/` 사용)
- [ ] 줌 레벨 확장 검토 (현재 [16, 20, 24])

---

## 프로젝트 관리 규칙

### 정책 변경 시
정책·설계 결정이 바뀌면 **이 GAME_DESIGN.md를 반드시 업데이트**한다.  
변경 내용: 설계 결정, 폐기된 방식, 새로운 제약 조건, 수치 변경 등.

### 히스토리 저장 (`히스토리 저장해줘`)
1. `_history/` 폴더에 전체 프로젝트를 zip으로 압축 저장
   - 파일명 형식: `age_of_war_YYYY-MM-DD.zip`
   - `_history/` 폴더 자체는 압축에서 제외
2. 이 GAME_DESIGN.md의 히스토리 로그를 업데이트

---

## 히스토리 로그

| 날짜 | 내용 |
|------|------|
| 2026-05-01 | 초기 아카이브. Python 버전 삭제, 지형 에셋 비활성화, 렌더링 스케일 기준을 128px → 24px로 정정 |
| 2026-05-01 | CLAUDE.md 작성. 전투 공식(공격/방어/혼란도/선두행), 장수 스탯, 지형·속도·밀도 보너스 상세 정리. 실시간 게임임을 명시 |
| 2026-05-02 | AoE 스타일 UI 전면 개편 — Noto Serif KR 폰트, 볼록 석재 버튼, 석판 HUD 카드(건강도 기반 HP바 색상), 석재 프레임 캔버스, 장수 스탯 뱃지 세로형, 기합 버튼 개선 |
| 2026-05-01 | Fog of War 시스템, 혼란도 시간 누적, 기합 스킬, UI/UX 재편(HUD 간소화, 패널 수직 구조, 속도 복귀 로직) 구현 |
| 2026-05-01 | FOW 시각 레이어 추가(어두운 오버레이 + 가변시야 반투명), 기합 이펙트 빨간색으로 통일, 혼란도 바차트 추가, 기합 fill 애니메이션 width 방식으로 수정, 전투 단계 체크 추가 |
| 2026-05-02 | FOW 레이캐스팅 전면 개편(산악 지형 시야 차단), 기합 쿨타임 200초로 확장, 후퇴 로직을 혼란도 기반으로 변경(0.7 이상 / 0.1마다 체크), 방어력에서 매력 보정 제거, 정지 버튼 토글 및 단축키(Space/Tab) 추가, 정지 중 적 추적 회전 기능, AoE 스타일 UI 전면 개편 |
| 2026-05-02 | 현재 버전 아카이브. 문서 파일명을 CLAUDE.md → GAME_DESIGN.md로 변경 |
| 2026-05-07 | 험준산악 크기 10×10 → 16×16 변경, mountain_forest_16.png 적용, 생성 시 50% 확률 추가. 화공: 강·습지 타일에서 파티클 생성·이동 차단. 수공: 시각효과 색상 흰색 반투명으로 변경, 말풍선 피해 시에만 표출, 피해량 조정. 신속: 쿨타임 80→60초, 지속시간 8→12초. 스킬 배정 확률 조정(기합 55%, 화공 20%, 신속 20%, 수공 5%). GAME_DESIGN.md 전면 검토·갱신. UX 플로우 개편(홈화면, 병력조정, 전투결과 화면 추가). 전투 패널에 실시간 공격력·방어력 표시. 속도별 공격 배율 0.95~1.05로 축소, 방어 보정 강화. 전투 종료 후 3초 딜레이 추가. 수공 피해량 20으로 조정 |
| 2026-05-08 | grassland 지형 추가(plain 위 군집 패치, 동일 게임 효과, 렌더 우선순위 dirt<plain<forest_base). generals.json 기반 장수 초상화 시스템 추가(패널 우측 98×98px 표시). 홈화면 배경 main.png 적용. 병력조정 화면 아군 카드 가로 배열·적군 컴팩트 리스트로 재편. 전투 패널 실시간 능력치 초상화 좌측·텍스트 우측 레이아웃 |
| 2026-05-08 | 서버/빌드 전환 시작. Vite 빌드 설정, Node/Express 서버 골격, `TECH_DESIGN.md`, 배포/에셋 문서, PixiJS 렌더러 전환용 `web/src/renderer` 골격 추가. 현재 실행 기준은 `web/app.js`로 유지 |
| 2026-05-09 | `ancient_infantry_helmet_walk.png` 5프레임 보행 스프라이트를 유닛 그래픽으로 적용. 기존 코드 생성 픽셀 유닛은 이미지 로드 실패 시 폴백으로 유지 |
| 2026-05-09 | 유닛 보행 스프라이트 표시 크기를 85%로 축소하고 적군 붉은 틴트를 제거. 선두행 전투 보너스 수치는 유지하되 선두행/기합 시각 후광의 크기와 투명도를 축소 |
| 2026-05-09 | 신규 스킬 사수(정위치 유닛 방어력 2배, 5초)와 신궁(원거리 사거리 2배, 5초) 추가. 병력 조정 화면에서 아군 장수 스킬 선택 가능. 장수 프로필에 `optionalSkills`를 추가해 화공/수공/신궁 선택 가능 여부를 관리 |
| 2026-05-09 | 이동 속도 `느림(SLOW)`의 이동 방식을 방향 고정 이동으로 변경. 느림 상태에서는 목표 이동/적 추적 중에도 진형 방향을 자동 회전하지 않음 |
| 2026-05-09 | `느림(SLOW)` 선택 시 "현재 진형을 유지한채 이동하라." 명령 말풍선 표출 추가 |
| 2026-05-09 | `느림(SLOW)` 이동속도 0.55→0.45 타일/초로 하향 |
| 2026-05-10 | `느림(SLOW)` 이동속도 0.45→0.30 타일/초로 추가 하향 |
| 2026-05-09 | 홈 화면 배경을 `assets/background/main.png`, `main1.png` 중 실행 시 랜덤 선택하도록 변경 |
| 2026-05-09 | 홈 화면 배경 이미지 레이어 투명도 40% 적용 |
| 2026-05-09 | 갱신된 `ancient_infantry_helmet_walk.png` 크기(50×20)에 맞춰 유닛 보행 스프라이트 분할을 5프레임으로 변경 |
| 2026-05-09 | 유닛 정지 스프라이트(`ancient_infantry_helmet_idle_1.png`)와 적군용 blue 스프라이트(`*_blue.png`) 적용. 정지 시 idle, 이동 시 walk 스프라이트 사용 |
| 2026-05-09 | 유닛 스프라이트 좌우 방향을 순간 `vx` 직접 판정에서 `visualFacingLeft` 유지 방식으로 변경. 정지 상태에서는 진형 방향 기준으로 고정해 좌우 반전 진동을 방지 |
| 2026-05-09 | 유닛 보너스 시각 효과 조정. 선두행/기합 보너스 후광은 더 강하게 표시하고, 선두행이 아닌 정위치 보너스에도 약한 후광 표시 추가 |
| 2026-05-09 | 전투 종료 처리 변경. 승패 조건 충족 시 즉시 시뮬레이션을 멈추지 않고 3초간 전투를 계속 진행한 뒤 `ended`로 전환하고 결과 화면 표시 |
| 2026-05-09 | 선두행이 아닌 정위치 보너스 후광 강도를 상향. 사수 효과를 정위치/선두행 방어 보너스 적용 후 최종 방어값 2배 방식으로 조정 |
| 2026-05-10 | 사수 지속시간을 `7 + 통솔/100 × 3`초로 변경해 통솔력이 높을수록 7~10초 범위에서 오래 유지되도록 조정 |
| 2026-05-10 | UI 개선. HUD 장수 선택 카드 클릭 레이어 보강, 오른쪽 패널 버튼 포커스 방지 및 Space/Tab 단축키 처리 보강, 전투 화면 캔버스 로딩 마스크 추가 |
| 2026-05-10 | 진형 중앙 유닛 위에 표시할 56×40px 6프레임 깃발 스프라이트시트 `assets/flags/formation_flag_red_sheet.png` 생성. 사각형 형태를 유지하고 약한 펄럭임만 표현 |
| 2026-05-10 | 우측 장수 초상화 영역에 98×98 크기를 유지한 불투명 석재 질감 inner bevel 프레임 추가. 프레임 오버레이가 초상화 가장자리를 덮어 음각 효과가 보이도록 적용 |
| 2026-05-10 | 신속/사수/신궁 지속 중 유닛에 기합과 동일한 붉은 후광 표시 추가. 사수는 정위치 유닛에만 후광 표시 |
| 2026-05-10 | 병력 조정 화면의 아군 스킬 선택 UI를 드롭다운에서 한글 버튼 선택 방식으로 변경 |
| 2026-05-10 | 병력 조정 화면 스킬 버튼 배열을 모든 장수에게 동일하게 표시하고, 선택 불가능한 스킬은 비활성화 처리 |
| 2026-05-10 | `assets/units/ancient_infantry_flag_spritesheet*.png`를 사용해 진형 중심 위에 팀별 깃발 렌더링을 시험 적용했으나 화면에서는 비활성화. 에셋은 유지 |
| 2026-05-10 | PixiJS WebGL 렌더러 부분 전환. FOW 블러(`ctx.filter=blur`)를 PixiJS BlurFilter+erase blend로 교체, 유닛 렌더링을 PixiJS Sprite 배치로 교체. PixiJS 미준비 시 Canvas 2D 폴백 유지 |
| 2026-05-11 | 데미지 판정 기준을 실제 병사 손실 수로 정리. 유닛 1개는 100명을 압축 표시하며, HUD 병력바/우측 표시 병력/전투 결과 통계/AI 병력 비교/혼란도 생존율/병력 이동 페널티를 `sum(100 - unit.damage)` 기준으로 계산 |
| 2026-05-11 | 병과 시스템 추가. 보병/기병을 장수 단위로 선택하며 기병은 인구 비용 4, 근접공격/이동속도 2배, 근접/원거리방어 1.5배, 기합 전용, 촘촘 대형 불가로 설정. 빠른 전투 아군 1·5번째 장수는 기병 2,500명으로 시작 |
| 2026-05-11 | 기병 스프라이트 규격을 6프레임, 기본 줌 표시 높이 22px로 반영. 기병 프레임 폭에 맞춰 렌더링 폭, 그림자/후광, 진형 간격, 충돌 반경을 병과별로 분리 |
| 2026-05-11 | 기병 진형 간격을 소폭 축소하고, 기병 유닛의 바닥 그림자를 제거 |
| 2026-05-11 | 기병 스프라이트시트 높이 변경(22px→25px)을 반영해 기병 표시 기준 높이와 말풍선 높이 계산을 25px 기준으로 갱신 |
| 2026-05-11 | 기병 근접/원거리 방어 배율을 1.5배에서 2배로 상향하고, 기병의 원거리 공격 능력을 제거 |
| 2026-05-11 | 기병 근접공격/방어 배율 3.0→ 확인 유지, 근접방어 4.0배로 상향, 최종 근접방어에 +10 고정 보너스 추가 (속도·혼란도 등 조건 무관). UI 패널 방어력 표시에도 +10 반영 |
| 2026-05-11 | 기병 충돌 반발력 2.5배 상향 (enemy formation과 충돌 시 hard/soft zone 모두 적용) |
| 2026-05-11 | 수공 피해 유닛 이동 범위 2~3타일 → 1~4타일로 변경 |
| 2026-05-11 | 혼란도 증가 로직을 평균 이탈 거리(avgSlotDist) 기반에서 정위치 이탈 비율(POSITION_DEFENSE_THRESHOLD=0.8 기준) 기반으로 변경. 계수 8.0 적용 (0% 이탈 시 1×, 100% 이탈 시 9×) |
| 2026-05-11 | 혼란도에 따른 유닛 이동속도 패널티 추가. 혼란도 50%에서 NORMAL 속도와 동일해지는 수준. 계수 1.4 적용 |
| 2026-05-11 | 반응 반경 패널티 완화. 기존 혼란도 0.6 이상 일괄 -40%에서 점진적 방식으로 변경 (혼란도 1.0에서 최대 -20%) |
| 2026-05-11 | 이동속도별 방어 보정 수치 축소. STOP +3.0→+2.0, SLOW +1.5→+1.0, FAST -1.5→-1.0 |
