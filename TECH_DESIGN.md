# 기술 설계

최종 갱신: 2026-05-17

## 목표 구조

이 프로젝트는 로컬 HTML 실행 전제에서 서버와 빌드가 있는 웹 프로젝트로 전환 중이다.

- 클라이언트 빌드: Vite
- 서버: Node.js + Express
- 렌더링: Canvas 기반 동작 보존 후 PixiJS WebGL 점진 이관
- 초기 데이터 저장: JSON
- 이후 데이터 저장: SQLite

## 현재 실행 기준

현재 최신 게임 구현은 `web/app.js`에 있다. 기존 모듈 파일인 `web/main.js`, `web/game-logic.js`, `web/renderer.js`, `web/input.js`는 참고용으로 남아 있을 수 있으나 최신 동작과 차이가 있을 수 있다.

전환 중에는 `app.js`를 안정 기준으로 유지하고, 이후 `web/src/` 구조로 기능을 분리한다.

## 서버 책임

초기 서버는 가볍게 유지한다.

- `dist/` 정적 파일 제공
- `/api/health` 상태 확인
- `/api/version` 빌드/버전 정보 제공

향후 확장 대상:

- 전투 결과 저장
- 리플레이 저장
- 역사 시나리오 진행 상태 저장
- SQLite 저장소 도입
- 멀티플레이 또는 비동기 기록 기반 기능

## 렌더링 전환 원칙

게임 상태는 시뮬레이션 좌표와 타일 좌표를 계속 사용한다. PixiJS는 표시 계층에만 해당하며 전투 계산, AI, 지형 효과, 충돌 계산은 렌더러 객체에 의존하지 않는다.

전환 순서:

1. 기존 Canvas 렌더링 동작 보존
2. PixiJS 렌더러 껍데기 추가
3. 카메라와 좌표 변환 연결
4. 유닛 스프라이트 렌더링 이관
5. Fog of War 이관
6. 지형과 효과 렌더링 추가 이관

## 역사 시나리오 구조

역사 시나리오는 빠른 전투와 다르게 정해진 데이터로 시작한다.

- 시나리오 데이터: `web/data/scenarios/*.json`
- 고정 지형 데이터: `web/data/scenarios/*_terrain.json`
- 지형 생성 스크립트: `scripts/generate-*.mjs`

역사 시나리오 모드 특징:

- `game.mode = "historical"`을 사용한다.
- 병력 조정 화면을 건너뛴다.
- 정해진 장수, 병과, 병력, 능력치, 스킬로 시작한다.
- 랜덤 지형 대신 시나리오 지형 JSON을 사용한다.
- 일반 승패와 별도로 퀘스트 완료 조건을 처리한다.
- 대화, 퀘스트 안내, 퀘스트 수행, 다음 단계 전환을 시나리오 런타임 상태로 관리한다.

## 칸나에 전투 구현

관련 파일:

- `web/data/scenarios/cannae.json`
- `web/data/scenarios/cannae_terrain.json`
- `scripts/generate-cannae-terrain.mjs`
- `web/terrain-json-viewer.html`

기술 포인트:

- 240x160 고정 지형을 사용한다.
- 북쪽 강, 중앙 평원, 남쪽 산지/초지대를 생성한다.
- 평지와 산지 경계는 노이즈를 사용해 직선감을 줄인다.
- 퀘스트 마커는 시나리오 데이터의 marker 정보를 사용한다.
- 퀘스트 박스 클릭 시 현재 단계의 주요 마커를 5초 동안 다시 보여준다.

## 칸나에 AI

- 1단계 로마군은 빠른 속도로 지정 집결선까지 이동한다.
- 1단계 완료 전 피해를 받으면 공격 행동을 우선한다.
- 2단계 로마 보병은 자신의 진행 방향과 가장 가까운 아군 진형을 추적한다.
- 2단계 로마 기병은 지정 위치를 지키며, 50% 이하가 되면 패주한다.
- 3단계 로마군은 혼란도 0.7 이상으로 상승한다.
- 3단계 로마 보병은 가장 앞선 로마 보병 위치를 기준으로 뭉치려 한다.

## 지형 JSON 뷰어

`web/terrain-json-viewer.html`은 개발용 도구다. 로컬 파일로 직접 열면 CORS 제한이 발생할 수 있으므로 Vite 개발 서버에서 접근한다.

```text
http://localhost:3000/terrain-json-viewer.html
```
