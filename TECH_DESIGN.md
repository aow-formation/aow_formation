# 기술 설계

## 목표 구조

이 프로젝트는 로컬 HTML 실행 전제에서 서버와 빌드가 있는 웹 프로젝트로 전환한다.

- 클라이언트 빌드: Vite
- 서버: Node.js + Express
- 렌더링 전환 대상: PixiJS WebGL
- 초기 저장소: JSON
- 후속 저장소: SQLite

## 현재 실행 기준

현재 최신 게임 구현은 `web/app.js`에 있다. 기존 모듈형 파일(`web/main.js`, `web/game-logic.js`, `web/renderer.js`, `web/input.js`)은 참고용으로 유지하되, 최신 동작과 차이가 있을 수 있다.

전환 중에는 `app.js`를 안정 기준으로 보존하고, 이후 `web/src/`로 기능을 분리한다.

## 렌더링 전환 원칙

게임 상태와 시뮬레이션 좌표는 계속 타일 좌표계를 사용한다. PixiJS는 표시 계층만 담당하며 전투 계산, AI, 지형 효과, 충돌 계산은 렌더러 객체에 의존하지 않는다.

전환 순서:

1. Canvas 렌더러 기준 동작 보존
2. PixiJS 렌더러 껍데기 추가
3. 카메라와 좌표 변환 이관
4. 지형 타일 렌더링 이관
5. 유닛과 선택 표시 이관
6. 스킬 이펙트와 Fog of War 이관

## 서버 책임

초기 서버는 얇게 유지한다.

- `dist/` 정적 파일 제공
- `/api/health` 상태 확인
- `/api/version` 빌드/버전 정보 제공

후속 확장:

- 전투 결과 저장
- 리플레이 저장
- 시나리오 저장
- SQLite 저장소 도입
